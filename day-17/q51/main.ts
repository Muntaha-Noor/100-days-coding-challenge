function calculate_area(length: number, width: number){
    return length * width;
}

const calculateAreaArrow = (length: number, width: number) =>
 length * width;

console.log(calculateAreaArrow(5, 7));